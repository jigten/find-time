export const calculateFreeTimes = (times: any, finalSchedule: string[], meetingDuration: number) => {
  console.log('finalSchedule: ', finalSchedule);
  console.log('times: ', times);
  console.log('meetingDuration: ', meetingDuration);
  return 1;
};
